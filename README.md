1. first open cmd terminal inside root directory.
2. run = cd backend
3. run = npm install
4. run = npm run dev

1. first open onther cmd terminal inside root directory.
2. run = npm install
3. run = npm run dev
